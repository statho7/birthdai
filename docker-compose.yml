services:
  # React/Vite Frontend
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: birthdai-frontend
    ports:
      - "8080:80"
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - birthdai-network

  # Python Voice AI Agent
  voice-ai-agent:
    build:
      context: ./voice-ai-agent
      dockerfile: Dockerfile
    container_name: birthdai-voice-agent
    ports:
      - "8000:8000"  # API server port
    env_file:
      - ./voice-ai-agent/.env.local
    volumes:
      - music-files:/app/generated_music  # Shared volume for music files
    restart: unless-stopped
    networks:
      - birthdai-network
    depends_on:
      - frontend

networks:
  birthdai-network:
    driver: bridge

volumes:
  music-files:
